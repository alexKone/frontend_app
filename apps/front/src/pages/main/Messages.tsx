import { useEffect, useState } from "react";


const Messages = () => {
  // useEffect(() => {

  // }, [])
  return (
    <section>
      <h3>mes messages</h3>
      {/* {messages.map(message => <li key={message.id}>{message.content} ({message.status}) - ({message.receiver.firstname} {message.receiver.lastname}) </li>)} */}
      {/* <h3>Messages reçus</h3> */}
      {/* {received.map((r: any, i: number) => <li key={i}>{r.content}</li>)} */}
    </section>

  )
}

export default Messages;
